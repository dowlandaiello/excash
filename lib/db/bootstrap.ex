defmodule Db.Bootstrap do
  @moduledoc """
  Bootstraps the global state from the connected peers.
  """

  @doc """
  Adopts / bootstraps the average state from the connected peers.
  """
  def synchronize_state() do
  end
end
